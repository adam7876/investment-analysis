services:
  - type: web
    name: ai-investment-analysis
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python ultra_minimal_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9 